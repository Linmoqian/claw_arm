# SO100 机械臂底层控制指南

不使用 LeRobot 库，直接使用 Feetech SCServo SDK 控制 SO100 机械臂。

## 概述

SO100 机械臂使用 **Feetech STS3215** 电机，通过串口通信。可以直接使用 `scservo_sdk` 进行控制，无需依赖 LeRobot。

## 安装依赖

```bash
pip install pyserial
```

`scservo_sdk` 通常已随 LeRobot 安装。

## 快速开始

### 1. 导入模块

```python
from scservo_sdk import PortHandler, PacketHandler
import time
```

### 2. 连接机械臂

```python
# 创建控制器
port_handler = PortHandler("COM7")  # 或 "/dev/ttyUSB0"
packet_handler = PacketHandler(0.0)

# 打开端口
port_handler.openPort()
port_handler.setBaudRate(1000000)  # 1 Mbps

print("已连接")
```

### 3. 控制电机

```python
# 启用扭矩
packet_handler.write1ByteTxRx(
    port_handler,
    motor_id,        # 1-6
    40,              # TORQUE_ENABLE 地址
    1                # 1 = 启用, 0 = 禁用
)

# 设置位置 (0-4095)
packet_handler.write2ByteTxRx(
    port_handler,
    motor_id,
    42,              # GOAL_POSITION 地址
    position         # 0-4095
)

# 读取位置
data, result, error = packet_handler.read2ByteTxRx(
    port_handler,
    motor_id,
    56               # PRESENT_POSITION 地址
)

if result == 0:  # 成功
    print(f"位置: {data}")
```

### 4. 断开连接

```python
# 禁用所有电机扭矩
for motor_id in range(1, 7):
    packet_handler.write1ByteTxRx(port_handler, motor_id, 40, 0)

# 关闭端口
port_handler.closePort()
```

## 完整示例

### 示例 1：基本运动

```python
from scservo_sdk import PortHandler, PacketHandler
import time

# 连接
port_handler = PortHandler("COM7")
packet_handler = PacketHandler(0.0)
port_handler.openPort()
port_handler.setBaudRate(1000000)

try:
    # 启用所有电机
    for motor_id in range(1, 7):
        packet_handler.write1ByteTxRx(port_handler, motor_id, 40, 1)

    # 移动到零点
    print("移动到零点...")
    for motor_id in range(1, 6):
        packet_handler.write2ByteTxRx(port_handler, motor_id, 42, 2047)
    packet_handler.write2ByteTxRx(port_handler, 6, 42, 2000)
    time.sleep(2)

    # 移动到位置 A
    print("移动到位置 A...")
    packet_handler.write2ByteTxRx(port_handler, 1, 42, 2500)
    packet_handler.write2ByteTxRx(port_handler, 2, 42, 2500)
    packet_handler.write2ByteTxRx(port_handler, 3, 42, 1500)
    time.sleep(2)

    # 读取位置
    for motor_id in range(1, 7):
        data, result, _ = packet_handler.read2ByteTxRx(
            port_handler, motor_id, 56
        )
        if result == 0:
            print(f"电机 {motor_id}: {data}")

finally:
    # 断开
    for motor_id in range(1, 7):
        packet_handler.write1ByteTxRx(port_handler, motor_id, 40, 0)
    port_handler.closePort()
```

### 示例 2：校准

```python
from scservo_sdk import PortHandler, PacketHandler

port_handler = PortHandler("COM7")
packet_handler = PacketHandler(0.0)
port_handler.openPort()
port_handler.setBaudRate(1000000)

# 1. 禁用扭矩
for motor_id in range(1, 7):
    packet_handler.write1ByteTxRx(port_handler, motor_id, 40, 0)

print("请将机械臂移动到中间位置...")
input("按 Enter 继续...")

# 2. 读取零点
zero_positions = {}
for motor_id in range(1, 7):
    data, result, _ = packet_handler.read2ByteTxRx(port_handler, motor_id, 56)
    if result == 0:
        zero_positions[motor_id] = data
        print(f"电机 {motor_id} 零点: {data}")

# 3. 测试运动范围
ranges = {}
for motor_id in range(1, 7):
    print(f"\n电机 {motor_id}:")
    input("  移动到最小位置，按 Enter...")
    data, _, _ = packet_handler.read2ByteTxRx(port_handler, motor_id, 56)
    min_pos = data

    input("  移动到最大位置，按 Enter...")
    data, _, _ = packet_handler.read2ByteTxRx(port_handler, motor_id, 56)
    max_pos = data

    ranges[motor_id] = (min_pos, max_pos)
    print(f"  范围: {min_pos} - {max_pos}")

# 保存校准数据
with open('calibration.txt', 'w') as f:
    f.write("zero_positions = " + str(zero_positions) + "\n")
    f.write("ranges = " + str(ranges) + "\n")

print("校准完成！数据已保存到 calibration.txt")

# 启用扭矩并断开
for motor_id in range(1, 7):
    packet_handler.write1ByteTxRx(port_handler, motor_id, 40, 1)

port_handler.closePort()
```


## 寄存器地址

常用寄存器地址：

| 地址 | 名称 | 说明 |
|------|------|------|
| 3 | ID | 电机 ID |
| 40 | TORQUE_ENABLE | 扭矩启用 (1=启用, 0=禁用) |
| 42 | GOAL_POSITION | 目标位置 (0-4095) |
| 56 | PRESENT_POSITION | 当前位置 (0-4095) |
| 46 | MOVING_SPEED | 移动速度 |

完整列表请参考 Feetech STS3215 数据手册。

## 电机 ID 映射

| ID | 关节 | 说明 |
|----|------|------|
| 1 | shoulder_pan | 底部旋转 |
| 2 | shoulder_lift | 大臂升降 |
| 3 | elbow_flex | 小臂弯曲 |
| 4 | wrist_flex | 手腕俯仰 |
| 5 | wrist_roll | 手腕旋转 |
| 6 | gripper | 夹爪开合 |

## 位置范围

- **最小值**: 0
- **最大值**: 4095
- **中间值**: ~2047

## 常见问题

### Q: 为什么电机不移动？

**A**: 检查：
1. 是否已启用扭矩 (TORQUE_ENABLE = 1)
2. 电源是否已接通
3. 位置值是否在有效范围内 (0-4095)

### Q: 如何设置速度？

**A**: 使用 MOVING_SPEED 寄存器：

```python
packet_handler.write2ByteTxRx(
    port_handler,
    motor_id,
    46,  # MOVING_SPEED
    speed  # 0-2400
)
```

### Q: 如何使用多电机？

**A**: 使用 GroupSyncWrite 或依次调用：

```python
# 依次设置
for motor_id in [1, 2, 3]:
    packet_handler.write2ByteTxRx(port_handler, motor_id, 42, position)
```

## 与 LeRobot 对比

| 特性 | 底层控制 | LeRobot |
|------|----------|---------|
| 依赖 | scservo_sdk | lerobot |
| 代码量 | 少 | 多 |
| 功能 | 基础控制 | 高级功能 |
| 相机集成 | 需自己实现 | 内置支持 |
| 数据记录 | 需自己实现 | 内置支持 |
| 校准 | 需自己实现 | 自动化 |
| 适用场景 | 简单控制 | 完整应用 |
