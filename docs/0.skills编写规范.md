# OpenClaw 技能编写规范
OpenClaw 的 Skill 遵循 AgentSkills 规范，本质上是一个包含元数据和说明文件的文件夹。

# SKILL.md 的格式：
文件名：skills/<skill-name>/SKILL.md
顶部 YAML frontmatter（最重要的是 name / description / metadata.openclaw）
正文用 Markdown 写“什么时候用、怎么用、示例命令/示例 JSON、注意事项”等内容。

skill 的 metadata.openclaw.requires 可以声明需要的 bins / env / config 等
用户侧配置在 ~/.openclaw/openclaw.json 的 skills 下（启用/禁用、注入 env、apiKey 便捷字段等）

# 快速创建skill

调用skill-create ,让openclaw自动生成skill

## 注意事项：

metadata：必须是单行 JSON 对象（这是 OpenClaw 解析器的特殊要求）。

requires：可以用它来限制环境。例如 "bins": ["python3"] 表示只有系统安装了 Python3 时，该 Skill 才会激活。

{baseDir}：在指令中使用此占位符来引用你文件夹内的脚本。

手动调用（斜杠命令）：如果在元数据中设置了 user-invocable: true，你可以在聊天框输入 /my-custom-skill 直接触发。

编写SKILL.md文件 [skill示例编写](skill的示例.md)

<img src=../docs/refences/image-1.png width=400>
